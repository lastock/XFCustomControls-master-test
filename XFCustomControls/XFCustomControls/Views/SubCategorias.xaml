<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="XFCustomControls.Views.SubCategorias"
             xmlns:ext="clr-namespace:XFCustomControls.Ext;assembly=XFCustomControls.Ext"
             Title="SubCategorias">
    <StackLayout Padding="10">
        <ext:ListView ItemsSource="{Binding Items}" 
            ItemTappedCommand="{Binding ItemTappedCommand}"
            >
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextCell Text="{Binding NomeSubCategoria}" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ext:ListView>
    </StackLayout>
</ContentPage>
